[toolchain]
anchor_version = "0.31.1"
solana_version = "2.1.0"

[features]
resolution = true
skip-lint = false

# Placeholder — update after `anchor build && anchor keys sync`
[programs.localnet]
mint_machine = "11111111111111111111111111111112"

[programs.devnet]
mint_machine = "11111111111111111111111111111112"

[registry]
url = "https://api.apr.dev"

[provider]
cluster = "localnet"
wallet = "~/.config/solana/id.json"

[scripts]
test = "yarn run ts-mocha -p ./tsconfig.json -t 1000000 tests/**/*.ts"
